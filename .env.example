# Supabase — shared iotty/Hermes database (project: ogodslwbdxidudymtvnn, formerly rvo-host-concierge slot)
SUPABASE_URL=https://ogodslwbdxidudymtvnn.supabase.co
# anon key is enough for the public site (insert-only RLS + counter RPC)
SUPABASE_ANON_KEY=
# service role key (Supabase dashboard → Settings → API) — only needed for admin/Hermes scripts
SUPABASE_SERVICE_ROLE_KEY=
